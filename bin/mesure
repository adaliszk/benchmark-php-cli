#!/usr/bin/env bash

for i in {1..100}; do

	{ time for k in {1..1000}; do 
		/usr/bin/php /srv/$1.php > /dev/null
	done; } 2>&1 | cat | sed 's/\t/:/g' | sed '/^\s*$/d' >> "/out/$CONTAINER_NAME-$1"
	
	e=$(($i*1000));
	printf "executed $e times\n"
done

